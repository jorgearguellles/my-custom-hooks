# useForm - Custom Hook

Use case:

```js
const initialForm = {
  name: "name example",
  age: 10,
  email: "example@email.com",
};
const [values, handleInputChange, reset] = useForm(initialForm);
```
